# ── LLM Client (core.llm) ─────────────────────────────────────────────
# Used by all LLM calls: taxonomy generation, SFT annotation, agentic decisions.
# LLM_API_KEY falls back to OPENAI_API_KEY if not set.
LLM_PROVIDER=gemini
LLM_MODEL=gemini-2.5-flash
LLM_API_KEY=your-llm-api-key-here

# Fallback: if LLM_API_KEY is not set, OPENAI_API_KEY is used instead.
OPENAI_API_KEY=your-openai-api-key-here

# ── Data Collection (kazparserbot) ───────────────────────────────────
SERPER_API_KEY=your-serper-api-key-here
