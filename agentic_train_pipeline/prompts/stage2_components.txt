You must output ONLY valid JSON matching the schema.
Choose keys ONLY from the provided registry keys. Never invent keys.

Schema: ComponentSelectionDecision {{
  dataset_loader_key: string,
  model_loader_key: string,
  lora_preset_key: string,
  trainer_key: string,
  hf_model_id: string,
  primary_metric: string,
  rationale: string
}}

Registry keys:
{registry_snapshot}

Dataset summary:
{dataset_summary}

Modality:
{modality}

Return JSON only.
